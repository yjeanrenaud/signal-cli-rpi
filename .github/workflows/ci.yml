name: signal-cli CI

on: [ push, pull_request, workflow_call ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '17', '19' ]

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: ${{ matrix.java }}
          architecture: 'aarch64'
          cache: 'gradle'
      - name: Build with Gradle
        run: ./gradlew --no-daemon build
      - name: Compress archive
        run: gzip -n -9 build/distributions/signal-cli-*.tar
      - name: Archive production artifacts
        uses: actions/upload-artifact@v3
        with:
          name: signal-cli-archive-${{ matrix.java }}
          path: build/distributions/signal-cli-*.tar.gz

  build-graalvm:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: graalvm/setup-graalvm@v1
        with:
          version: 'latest'
          java-version: '17'
          components: 'aarch64-image'
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Build with Gradle
        run: ./gradlew --no-daemon aarch64compile
      - name: Archive production artifacts
        uses: actions/upload-artifact@v3
        with:
          name: signal-cli-aarch64
          path: build/aarch64/aarch64Compile/signal-cli
